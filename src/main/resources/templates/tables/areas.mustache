<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Trucking company database manager</title>
    <meta http-equiv="Content-Type" content="text/html; charseet=UTF-8" />
    <style type="text/css">
        .header {
            background-color: #E5C821;
            padding: 15px;
            text-align: center;
        }
        .caption {
            font-size: 20px;
            font-family: Arial, Helvetica, sans-serif;
            color: white;
            background-color: #A0A0A0;
            padding: 25px;
        }
        th, td {
            height: 30px;
            font-family: Arial, Helvetica, sans-serif;
            border-style: solid;
            border-color: #A0A0A0;
            border-width: 1px;
        }
        .button_td {
            height: 30px;
            width: 100px;
            border-style: solid;
            border-color: white;
            border-width: 1px;
            background-color: #A0A0A0;
        }
        .button_th {
            height: 30px;
            width: 100px;
            border-style: solid;
            border-color: #A0A0A0;
            border-width: 1px;
        }
        table {
            width: 100%;
            font-size: 14px;
            border-radius: 15px;
            border-collapse: collapse;
            text-align: center;
        }
        .button {
            border: none;
            background-color: #A0A0A0;
            font-weight: bold;
            color: white;
        }
        .button:hover {
            cursor: pointer;
        }
        .insert {
            height: 90px;
            background-color: #303030;
        }
        .field {
            width: 9%;
            height: 24px;
            margin-top: 30px;
        }
        label {
            color: white;
            font-family: Arial, Helvetica, sans-serif;
            margin-left: 1%;
        }
        .add_button {
            border: none;
            background-color: #303030;
            font-weight: bold;
            color: white;
            font-size: 24px;
            float: right;
            margin-top: 30px;
            margin-right: 1%;
            height: 24px;
            width: 6%;
        }
        .add_button:hover {
            cursor: pointer;
        }
        .back {
            background-color: #E5C821;
            padding: 15px;
        }
        .back_button {
            background-color: #E5C821;
            border-color: white;
            border-radius: 4px;
            font-family: Arial, Helvetica, sans-serif;
            color: white;
            font-size: 24px;
            font-weight: bold;
        }
        .back_button:hover {
            cursor: pointer;
        }
    </style>
</head>
<body>
<div class="back">
    <form method="get" action="/menu"><button class="back_button">BACK</button></form>
</div>
<div class="header">
    <h2 style="font-family: Arial, Helvetica, sans-serif; color: white; font-size: 400%;">
        Trucking company database manager
    </h2>
</div>
<div class="insert">
    <form method="post" action="/addArea">
        <label>
            Начальник
            <select class="field" required name="chief">
                {{#employees}}
                    <option value={{empId}}>{{empId}}</option>
                {{/employees}}
            </select>
        </label>
        <button class="add_button" type="submit">ADD</button>
    </form>
</div>
<div>
    <table>
        <caption class="caption"><b>УЧАСТКИ</b></caption>
        <tr>
            <th>ID участка</th>
            <th>ID начальника</th>
            <th>ФИО начальника</th>
            <th class="button_th"></th>
            <th class="button_th"></th>
        </tr>
        {{#areas}}
            <tr>
                <td>{{areaId}}</td>
                <td>{{chief.empId}}</td>
                <td>{{chief.name}}</td>
                <td class="button_td">
                    <form>
                        <button class="button">EDIT</button>
                    </form>
                </td>
                <td class="button_td">
                    <form method="post" action="/deleteArea">
                        <button name="areaId" value={{areaId}} type="submit" class="button">DELETE</button>
                    </form>
                </td>
            </tr>
        {{/areas}}
    </table>
</div>
</body>
</html>