<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Trucking company database manager</title>
    <meta http-equiv="Content-Type" content="text/html; charseet=UTF-8" />
    <link href="/static/table.css" type="text/css" rel="stylesheet">
</head>
<body>
<div class="top">
    <form method="get" action="/logout"><button class="logout_button">LOGOUT</button></form>
</div>
<div class="header">
    <a style="text-decoration:none" href="/menu">
        <h2 style="font-family: Arial, Helvetica, sans-serif; color: white; font-size: 400%;">
            Trucking company database manager
        </h2>
    </a>
</div>
<div>
<div class="insert">
    <form method="post" action="/addRide">
        <div>
        <label>
            <select class="field" required name="route">
                {{#routes}}
                    <option value={{routeId}}>{{routeId}}</option>
                {{/routes}}
            </select>
            Маршрут
        </label>
        </div>
        <div>
        <label>
            <select class="field" required name="transport">
                {{#transport}}
                    <option value={{id}}>{{id}}</option>
                {{/transport}}
            </select>
            Транспорт
        </label>
        </div>
        <div>
        <label>
            <input required class="field" type="date" min="2015-01-01" max="2020-12-31" name="date" value="2020-12-31">
            Дата
        </label>
        </div>
        <button class="add_button" type="submit">Add</button>
    </form>
</div>
<div style="float: left; width: 78%;">
    <table>
        <caption class="caption"><b>ПОЕЗДКИ</b></caption>
        <tr>
            <th>ID</th>
            <th>Дата</th>
            <th>Маршрут</th>
            <th>Транспорт</th>
            <th class="button_th"></th>
            <th class="button_th"></th>
        </tr>
        {{#rides}}
            <tr>
                <td>{{rideId}}</td>
                <td>{{date}}</td>
                <td>{{route.routeId}}</td>
                <td>{{transport.id}}</td>
                <td class="button_td">
                    <form>
                        <button class="button">EDIT</button>
                    </form>
                </td>
                <td class="button_td">
                    <form method="post" action="/deleteRide">
                        <button name="rideId" value={{rideId}} type="submit" class="button">DELETE</button>
                    </form>
                </td>
            </tr>
        {{/rides}}
    </table>
</div>
</div>
</body>
</html>